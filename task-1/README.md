# Task 1 - LLM Tool Calling

Simple script that calls OpenAI with a calculator tool.

## Setup

1. Copy `.env.example` to `.env` and add your API key
2. Run `make install`

## Run

```bash
make run
```

